# Build VibePM as a native, local-first macOS application

VibePM will use SwiftUI for its macOS interface and SwiftData for on-device persistence. This prioritizes native Mac interaction, offline availability, and a small operational footprint over immediate cross-platform support; cloud synchronization and collaboration remain later, explicit product decisions rather than first-release requirements.

